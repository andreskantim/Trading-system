PARCHE PARA walkforward_donchian_mod.py
=======================================

Para que walkforward_donchian_mod.py genere resultados IDÉNTICOS
a walkforward_permutation.py, aplica el siguiente cambio:

LÍNEA 45:
---------
ANTES:
    wf_perm = get_permutation(df_perm, start_index=train_window)

DESPUÉS:
    wf_perm = get_permutation(df_perm, start_index=train_window, seed=perm_i)

LÍNEA 128-129:
--------------
ANTES:
    args_list = [(i, df_dict, train_window, real_wf_pf)
                 for i in range(n_permutations)]

DESPUÉS:
    args_list = [(i, df_dict, train_window, real_wf_pf)
                 for i in range(n_permutations)]

(No cambiar - ya usa 'i' como índice)

LÍNEA 34:
---------
ACTUALIZAR la firma de la función:

ANTES:
def process_walkforward_permutation(args):
    """Procesa una permutación individual de walk-forward"""
    perm_i, df_dict, train_window, real_wf_pf = args

DESPUÉS:
def process_walkforward_permutation(args):
    """Procesa una permutación individual de walk-forward"""
    perm_i, df_dict, train_window, real_wf_pf = args

(Ya está bien - perm_i se extrae correctamente)

=======================================

CON ESTE CAMBIO:
- walkforward_donchian_mod.py usará seed=perm_i
- walkforward_permutation.py ya usa seed=perm_i
- Ambos generarán EXACTAMENTE los mismos resultados

VERIFICACIÓN:
1. Aplicar el parche
2. Ejecutar: python walkforward_donchian_mod.py donchian.py
3. Ejecutar: python walkforward_permutation.py donchian
4. Comparar: Deberían tener el MISMO número de "Mejores que real"
